node_modules
dist
.env
.env.*
!.env.example
logs
*.log
.DS_Store
*.tsbuildinfo

# Committed SQL migrations are the source of truth (drizzle-orm migrate runner).
# They are intentionally tracked — do NOT ignore them.

# Pre-deploy DB backups written on the VPS by the deploy workflow
backups/
